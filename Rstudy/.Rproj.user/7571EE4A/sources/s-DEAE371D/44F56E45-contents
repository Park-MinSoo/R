#문제 9
testdata2 <- scan("data/iotest2.txt", what="",encoding="UTF-8")
freq <- table(testdata2) #table은 갯수를 세주는 전용 함수이다.
which.max(freq)
names(which.max(freq))
cat("가장 많이 등장한 단어는",names(which.max(freq)),"입니다.","\n")

